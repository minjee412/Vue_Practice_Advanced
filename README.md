[데이터 호출 시점]

1. 컴포넌트 라이프 사이클 훅

   - created : 컴포넌트가 생성 되자마자 호출되는 로직

2. 라우터 네비게이션 가드
   - 특정 url로 접근하기 전에 동작을 정의하는 속성(함수)
     (화면의 정보가 다 불려 왔을때는, 깔끔하게 화면이 나오겠지만
     그 중간에 기다리는 과정의 ux를 어떻게 가져갈 것인가 생각해야 한다. )
